# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: containerapps-builtinauth-bicep
metadata:
  template: containerapps-builtinauth-bicep0.1.0
requiredVersions:
    azd: ">= 1.12.0"

infra:
  parameters:
    servicePrincipalClientId: "${SERVICE_PRINCIPAL_CLIENT_ID}"
    servicePrincipalClientSecret: "${SERVICE_PRINCIPAL_CLIENT_SECRET}"

services:
  aca:
    project: .
    language: py
    host: containerapp
    docker:
      remoteBuild: true
